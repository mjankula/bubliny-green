apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: bubliny-green
  name: bubliny-green
  namespace: mjankula-ns-2
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: bubliny-green
    weight: 100
